# Machine-specific — points at this computer's Android SDK install
local.properties

# Release signing. The upload key is the only thing that can ever publish an update to the
# listing, and its passwords sit next to it — neither goes anywhere near the repository.
keystore.properties
*.jks
*.keystore

# Build outputs
*.apk
*.aab
*.ap_
*.dex
**/build/
.gradle/
.kotlin/

# IDE
.idea/
*.iml
.DS_Store

# Gradle wrapper jar is fetched on demand; keep the properties/scripts only
gradle/wrapper/gradle-wrapper.jar

# Log/temp
*.log
captures/
.cxx/
